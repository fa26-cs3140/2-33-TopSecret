TOP SECRET - CLASS AND RESPONSIBILITY DIAGRAM

                         command-line arguments
                                   |
                                   v
                         +--------------------+
                         |     TopSecret      |
                         |      main()        |
                         +---------+----------+
                                   |
                                   v
                 +----------------------------------+
                 | UserInterface (Team Member A)    |
                 | + run(args): String              |
                 | validates input / formats output |
                 +----------------+-----------------+
                                  |
                                  | uses
                                  v
                 +----------------------------------+
                 | <<interface>> ProgramControl     |
                 | + listFiles(): List<String>      |
                 | + getFileContents(...): String   |
                 +----------------+-----------------+
                                  ^
                                  | implements
                 +----------------+-----------------+
                 | ProgramControlImpl (Member B)    |
                 | coordinates files and ciphering  |
                 +----------+-----------------------+
                            | uses               | uses
                            v                    v
          +----------------------------+   +---------------------------+
          | <<interface>> FileHandler  |   | <<interface>> Cipher      |
          | + getFileList(): String    |   | + decipher(...): String   |
          | + readFile(int): String    |   +-------------+-------------+
          | + readKey(String): String |                 ^
          +-------------+--------------+                 | implements
                        ^                    +------------+-------------+
                        | implements         | SubstitutionCipher (D)  |
          +-------------+--------------+     | validates and deciphers |
          | FileHandlerImpl (Member C) |     +------------+-------------+
          | reads data/ and ciphers/   |                  |
          | handles filesystem errors  |<-----------------+
          +----------------------------+   uses FileHandler for key data

Responsibility boundaries
- A handles command-line input and displayed output.
- B coordinates requests without directly accessing files or deciphering text.
- C owns all filesystem access and returns safe failure values.
- D validates key data and deciphers mission text without direct file access.
